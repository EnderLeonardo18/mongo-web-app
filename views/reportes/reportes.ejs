<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Reportes - Codecyt</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

        <%- include('../partials/navbar') %>

    <div class="container py-5">
        <h1 class="mb-4 text-secondary">📊 Reportes del Sistema</h1>

        <table class="table table-striped bg-white shadow-sm">
            <thead class="table-dark">
                <tr>
                    <th>Título</th>
                    <th>Generado por</th>
                    <th>Fecha</th>
                    <th class="text-center">Acciones</th>
                </tr>
            </thead>
            <tbody>
                <% reportes.forEach(r => { %>
                <tr class="align-middle">
                    <td><%= r.titulo %></td>
                    <td><%= r.generadoPor ? r.generadoPor.nombre : 'Sistema' %></td>
                    <td><%= new Date(r.fechaGeneracion).toLocaleDateString() %></td>
                    <td class="text-center">
                        <a href="/reportes/<%= r._id %>" class="btn btn-sm btn-outline-primary">Ver Detalles</a>
                    </td>
                </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</body>
</html>