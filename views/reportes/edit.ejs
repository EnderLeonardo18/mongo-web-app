<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Editar Reporte - AgroSistema</title>
</head>
<body class="bg-gray-100">
    <%- include('../partials/navbar') %>

    <div class="container mx-auto p-6 max-w-4xl">
        <div class="bg-white rounded-2xl shadow-xl overflow-hidden border-b-8 border-emerald-600">
            <div class="p-8">
                <h2 class="text-3xl font-bold text-gray-800 mb-8 border-l-8 border-emerald-600 pl-4">✏️ Editar Informe</h2>

                <form action="/reportes/<%= reporte._id %>?_method=PUT" method="POST" class="space-y-6">
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <div class="md:col-span-2">
                            <label class="block text-xs font-bold text-emerald-700 uppercase mb-2">Título del Informe</label>
                            <input type="text" name="titulo" value="<%= reporte.titulo %>" 
                                class="w-full border-2 p-3 rounded-lg focus:ring-4 focus:ring-emerald-100 outline-none transition" required>
                        </div>
                        <div>
                            <label class="block text-xs font-bold text-emerald-700 uppercase mb-2">Categoría</label>
                            <input type="text" name="tipoReporte" value="<%= reporte.tipoReporte %>" 
                                class="w-full border-2 p-3 rounded-lg focus:border-emerald-500 outline-none transition">
                        </div>
                        <div>
                            <label class="block text-xs font-bold text-emerald-700 uppercase mb-2">Autor Responsable</label>
                            <input type="text" name="autor" value="<%= reporte.autor %>" 
                                class="w-full border-2 p-3 rounded-lg focus:border-emerald-500 outline-none transition">
                        </div>
                        <div class="md:col-span-2">
                            <label class="block text-xs font-bold text-emerald-700 uppercase mb-2">Contenido del Reporte</label>
                            <textarea name="contenido" 
                                class="w-full border-2 p-4 rounded-lg h-60 outline-none focus:border-emerald-500 focus:ring-4 focus:ring-emerald-50 transition"><%= reporte.contenido %></textarea>
                        </div>
                    </div>

                    <div class="flex items-center gap-4 pt-4">
                        <button type="submit" class="bg-emerald-600 text-white px-10 py-4 rounded-lg font-black hover:bg-emerald-800 transition shadow-lg uppercase text-sm">
                            Actualizar Reporte
                        </button>
                        <a href="/reportes" class="text-gray-500 font-bold hover:text-gray-800 transition text-sm">Descartar Cambios</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
</html>