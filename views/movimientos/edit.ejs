<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Editar Movimiento</title>
</head>
<body class="bg-gray-100">
    <%- include('../partials/navbar') %>
    <div class="container mx-auto max-w-lg mt-12 bg-white p-8 rounded-2xl shadow-xl border-t-4 border-blue-500">
        <h2 class="text-2xl font-bold mb-2 text-gray-800">Modificar Movimiento</h2>
        <p class="text-sm text-gray-500 mb-6">Corrija los datos del registro seleccionado.</p>

        <form action="/movimientos/<%= movimiento._id %>?_method=PUT" method="POST" class="space-y-5">
            <div>
                <label class="block font-bold text-gray-700 text-sm mb-1">Identificador Saco</label>
                <input type="text" name="identificadorSaco" value="<%= movimiento.identificadorSaco %>" class="w-full border p-3 rounded-lg bg-gray-50 focus:ring-2 focus:ring-blue-400 outline-none">
            </div>
            
            <div>
                <label class="block font-bold text-gray-700 text-sm mb-1">Nombre Operador</label>
                <input type="text" name="nombreUsuario" value="<%= movimiento.nombreUsuario %>" class="w-full border p-3 rounded-lg focus:ring-2 focus:ring-blue-400 outline-none">
            </div>

            <div class="grid grid-cols-2 gap-4">
                <div>
                    <label class="block font-bold text-gray-700 text-sm mb-1">Tipo</label>
                    <select name="tipo" class="w-full border p-3 rounded-lg">
                        <option value="Entrada" <%= movimiento.tipo === 'Entrada' ? 'selected' : '' %>>Entrada</option>
                        <option value="Salida" <%= movimiento.tipo === 'Salida' ? 'selected' : '' %>>Salida</option>
                    </select>
                </div>
                <div>
                    <label class="block font-bold text-gray-700 text-sm mb-1">Cantidad</label>
                    <input type="number" name="cantidad" value="<%= movimiento.cantidad %>" class="w-full border p-3 rounded-lg focus:ring-2 focus:ring-blue-400 outline-none">
                </div>
            </div>

            <div class="flex gap-4 pt-4">
                <button type="submit" class="flex-1 bg-blue-600 text-white py-3 rounded-xl font-bold hover:bg-blue-700 shadow-lg transition">Actualizar</button>
                <a href="/movimientos" class="flex-1 bg-gray-100 text-gray-600 py-3 rounded-xl font-bold text-center hover:bg-gray-200">Cancelar</a>
            </div>
        </form>
    </div>
</body>
</html>