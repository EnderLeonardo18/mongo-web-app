<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Banco de Semillas - Codecyt</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

        <%- include('../partials/navbar') %>

    <div class="container py-5">
        <h1 class="mb-4 text-info">🌱 Banco de Semillas</h1>

        <div class="card mb-4 p-4 shadow-sm border-0">
            <form action="/semillas" method="POST" class="row g-3">
                <div class="col-md-4"><input name="nombreCientifico" class="form-control" placeholder="Nombre Científico" required></div>
                <div class="col-md-4"><input name="variedad" class="form-control" placeholder="Variedad" required></div>
                <div class="col-md-2"><button class="btn btn-info w-100">Guardar</button></div>
            </form>
        </div>

        <table class="table table-hover bg-white shadow-sm">
            <thead class="table-info">
                <tr><th>Nombre Científico</th><th>Variedad</th></tr>
            </thead>
            <tbody>
                <% semillas.forEach(semi => { %>
                <tr>
                    <td><i><%= semi.nombreCientifico %></i></td>
                    <td><%= semi.variedad %></td>
                </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</body>
</html>