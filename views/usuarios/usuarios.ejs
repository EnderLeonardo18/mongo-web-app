<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/css/tailwind.min.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Gestión de Usuarios - AgroSistema</title>
</head>
<body class="bg-gray-50">
    <%- include('../partials/navbar') %>

    <div class="container mx-auto p-6">
        <h2 class="text-3xl font-extrabold text-emerald-800 mb-6">👥 Panel de Control de Usuarios</h2>
        
        <div class="bg-white p-8 rounded-2xl shadow-xl mb-10 border-t-8 border-emerald-600">
            <h3 class="text-lg font-bold mb-4 text-gray-700">Registrar Nuevo Usuario</h3>
            <form action="/usuarios" method="POST" class="grid grid-cols-1 md:grid-cols-12 gap-4">
                <div class="md:col-span-4">
                    <label class="block text-xs font-black text-emerald-900 mb-2 uppercase">Nombre Completo</label>
                    <input type="text" name="nombre" class="w-full border-2 p-3 rounded-xl outline-none focus:border-emerald-600" placeholder="Ej: Juan Pérez" required>
                </div>
                <div class="md:col-span-4">
                    <label class="block text-xs font-black text-emerald-900 mb-2 uppercase">Correo Electrónico</label>
                    <input type="email" name="correo" class="w-full border-2 p-3 rounded-xl outline-none focus:border-emerald-600" placeholder="correo@ejemplo.com" required>
                </div>
                <div class="md:col-span-2">
                    <label class="block text-xs font-black text-emerald-900 mb-2 uppercase">Rol</label>
                    <select name="rol" class="w-full border-2 p-3 rounded-xl bg-white outline-none focus:border-emerald-600">
                        <option value="Trabajador">Trabajador</option>
                        <option value="Administrador">Administrador</option>
                    </select>
                </div>
                <div class="md:col-span-2 flex items-end">
                    <button type="submit" class="w-full bg-emerald-700 text-white py-3 rounded-xl font-black hover:bg-emerald-900 shadow-lg uppercase tracking-widest transition">
                        Registrar
                    </button>
                </div>
            </form>
        </div>

        <div class="flex justify-between mb-6 bg-white p-4 rounded-xl shadow-sm border border-gray-100">
            <form action="/usuarios" method="GET" class="flex gap-2">
                <input 
                    type="text" name="search" value="<%= search %>" 
                    placeholder="Buscar por nombre o correo..." 
                    class="border-2 p-2 rounded-xl w-64 focus:border-blue-500 outline-none">
                <button type="submit" class="bg-blue-600 text-white px-6 py-2 rounded-xl font-bold hover:bg-blue-700 transition">
                    Buscar
                </button>
            </form>
        </div>

        <div class="bg-white rounded-2xl shadow-lg overflow-hidden border border-gray-100">
            <table class="w-full">
                <thead class="bg-emerald-50 text-emerald-900 border-b">
                    <tr>
                        <th class="p-5 text-left">Nombre</th>
                        <th class="p-5 text-left">Correo</th>
                        <th class="p-5 text-left">Rol</th>
                        <th class="p-5 text-center">Estado</th>
                        <th class="p-5 text-center">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <% usuarios.forEach(user => { %>
                    <tr class="border-b hover:bg-emerald-50/50 transition">
                        <td class="p-5 font-bold text-gray-800"><%= user.nombre %></td>
                        <td class="p-5 text-gray-600"><%= user.correo %></td>
                        <td class="p-5">
                            <span class="px-3 py-1 rounded-full text-xs font-bold <%= user.rol === 'Administrador' ? 'bg-purple-100 text-purple-700' : 'bg-orange-100 text-orange-700' %>">
                                <%= user.rol %>
                            </span>
                        </td>
                        <td class="p-5 text-center text-sm">
                            <%= user.activo ? '✅ Activo' : '❌ Inactivo' %>
                        </td>
                        <td class="p-5 text-center">
                            <div class="flex justify-center gap-4">
                                <a href="/usuarios/<%= user._id %>" class="text-blue-500 font-bold hover:underline">Editar</a>
                                <a href="/usuarios/delete/<%= user._id %>" class="text-red-400 font-bold hover:underline" onclick="return confirm('¿Eliminar usuario?')">Borrar</a>
                            </div>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>