<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Editar Usuario</title>
</head>
<body class="bg-gray-50">
    <%- include('../partials/navbar') %>
    <div class="container mx-auto max-w-lg mt-10 bg-white p-8 rounded-xl shadow-lg border border-gray-100">
        <h2 class="text-2xl font-bold mb-6 text-emerald-700 border-b pb-2">Editar Perfil de Usuario</h2>
        
        <form action="/usuarios/<%= usuario._id %>?_method=PUT" method="POST" class="space-y-4">
            <div>
                <label class="block text-sm font-semibold text-gray-700">Nombre Completo</label>
                <input type="text" name="nombre" value="<%= usuario.nombre %>" class="w-full border p-2 rounded mt-1 focus:ring-2 focus:ring-emerald-500 outline-none" required>
            </div>
            <div>
                <label class="block text-sm font-semibold text-gray-700">Correo Electrónico</label>
                <input type="email" name="correo" value="<%= usuario.correo %>" class="w-full border p-2 rounded mt-1 focus:ring-2 focus:ring-emerald-500 outline-none" required>
            </div>
            <div>
                <label class="block text-sm font-semibold text-gray-700">Rol del Sistema</label>
                <select name="rol" class="w-full border p-2 rounded mt-1 bg-white">
                    <option value="Administrador" <%= usuario.rol === 'Administrador' ? 'selected' : '' %>>Administrador</option>
                    <option value="Trabajador" <%= usuario.rol === 'Trabajador' ? 'selected' : '' %>>Trabajador</option>
                </select>
            </div>
            <div class="flex items-center gap-3 py-2 bg-gray-50 p-3 rounded">
                <input type="checkbox" name="activo" value="true" <%= usuario.activo ? 'checked' : '' %> class="w-5 h-5 text-emerald-600 rounded">
                <label class="text-sm font-bold text-gray-700">Cuenta Activa</label>
            </div>
            <div class="flex gap-4 pt-4">
                <button type="submit" class="flex-1 bg-emerald-600 text-white py-3 rounded-lg font-bold hover:bg-emerald-700 transition shadow-md">Guardar Cambios</button>
                <a href="/usuarios" class="flex-1 bg-gray-200 text-center py-3 rounded-lg font-bold hover:bg-gray-300 transition">Cancelar</a>
            </div>
        </form>
    </div>
</body>
</html>