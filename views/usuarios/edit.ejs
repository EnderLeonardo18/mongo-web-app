<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/css/tailwind.min.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Editar Usuario - AgroSistema</title>
</head>
<body class="bg-gray-50">
    <%- include('../partials/navbar') %>

    <div class="container mx-auto max-w-lg mt-10">
        <h2 class="text-3xl font-extrabold text-emerald-800 mb-6 text-center">👤 Editar Perfil</h2>
        
        <div class="bg-white p-8 rounded-2xl shadow-xl border-t-8 border-emerald-600">
            <form action="/usuarios/<%= usuario._id %>?_method=PUT" method="POST" class="space-y-5">
                
                <div>
                    <label class="block text-xs font-black text-emerald-900 mb-2 uppercase">Nombre Completo</label>
                    <input type="text" name="nombre" value="<%= usuario.nombre %>" 
                           class="w-full border-2 p-3 rounded-xl outline-none focus:border-emerald-600" required>
                </div>

                <div>
                    <label class="block text-xs font-black text-emerald-900 mb-2 uppercase">Correo Electrónico</label>
                    <input type="email" name="correo" value="<%= usuario.correo %>" 
                           class="w-full border-2 p-3 rounded-xl outline-none focus:border-emerald-600" required>
                </div>

                <div>
                    <label class="block text-xs font-black text-emerald-900 mb-2 uppercase">Rol del Sistema</label>
                    <select name="rol" class="w-full border-2 p-3 rounded-xl bg-white outline-none focus:border-emerald-600">
                        <option value="Administrador" <%= usuario.rol === 'Administrador' ? 'selected' : '' %>>Administrador</option>
                        <option value="Trabajador" <%= usuario.rol === 'Trabajador' ? 'selected' : '' %>>Trabajador</option>
                    </select>
                </div>

                <div class="flex items-center gap-3 py-3 px-4 bg-emerald-50 rounded-xl border border-emerald-100">
                    <input type="checkbox" name="activo" value="true" <%= usuario.activo ? 'checked' : '' %> 
                           class="w-5 h-5 text-emerald-600 rounded border-gray-300 focus:ring-emerald-500">
                    <label class="text-sm font-bold text-emerald-900">Cuenta Activa</label>
                </div>

                <div class="flex gap-4 pt-4">
                    <button type="submit" class="flex-1 bg-emerald-700 text-white py-4 rounded-xl font-black hover:bg-emerald-900 shadow-lg uppercase tracking-widest transition">
                        Guardar
                    </button>
                    <a href="/usuarios" class="flex-1 bg-gray-200 text-gray-700 text-center py-4 rounded-xl font-black hover:bg-gray-300 uppercase tracking-widest transition">
                        Cancelar
                    </a>
                </div>
            </form>
        </div>
    </div>
</body>
</html>